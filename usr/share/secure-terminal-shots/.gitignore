shots/
shots-wayland/
# per-case payload files the capture reproduces from the terminal-poc-corpus at run
# time (plus the inline notify / random cases); the demo `cat`s these -- it never runs
# a script. Not committed (raw escape / non-ASCII bytes); the corpus is their source.
*.payload
