shots/
shots-wayland/
# generated hostile-DATA log files (deterministic, from make-*-log.sh); the demo
# `cat`s these -- it never runs a script. Not committed (they carry raw escape /
# non-ASCII bytes; the ASCII creators are the committed source).
hostile.log
homoglyph.log
