name: fixture
on:
  push:
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
jobs:
  work:
    if: false
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: printf 'x\n'
